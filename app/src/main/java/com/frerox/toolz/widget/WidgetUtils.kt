package com.frerox.toolz.widget

import android.content.Context
import android.widget.RemoteViews
import com.frerox.toolz.data.settings.SettingsRepository

object WidgetUtils {
    fun applyTheme(context: Context, views: RemoteViews, settingsRepository: SettingsRepository) {
        // Basic theme application - can be expanded as needed
        // For now, it's a placeholder to fix compilation errors
    }
}
